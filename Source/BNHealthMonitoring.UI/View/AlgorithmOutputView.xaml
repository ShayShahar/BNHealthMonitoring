<UserControl x:Class="BNHealthMonitoring.UI.View.AlgorithmOutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BNHealthMonitoring.UI.View"
             xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="950"
             DataContext="{Binding AlgorithmOutput, Source={StaticResource Locator}}">
    <Grid>
        <wpf:CartesianChart Series="{Binding Results}" 
                            LegendLocation="None"
                            Margin="2,0,2,0"
                            Foreground="Black"
                            Background="#2c2f30">
            <wpf:CartesianChart.AxisX>
                <wpf:Axis Title="COMPONENT" Labels="{Binding Components}"></wpf:Axis>
            </wpf:CartesianChart.AxisX>
            <wpf:CartesianChart.AxisY>
                <wpf:Axis Title="%" Labels="{Binding Labels}"></wpf:Axis>
            </wpf:CartesianChart.AxisY>
        </wpf:CartesianChart>
    </Grid>
</UserControl>
